# Remove llm247 V1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the legacy `src/llm247/` implementation and make the repository V2-only.

**Architecture:** The repository will keep `llm247_v2` as the sole runtime package, test target, and documented architecture. All legacy V1 runtime, test, and script entry points will be deleted, and project conventions will be updated to reflect a single supported implementation.

**Tech Stack:** Python 3, `unittest`, shell scripts, React/Vite frontend docs/build.

---

### Task 1: Lock the deletion scope in docs

**Files:**
- Create: `docs/plans/2026-03-06-remove-llm247-v1-design.md`
- Create: `docs/plans/2026-03-06-remove-llm247-v1.md`

**Step 1: Record the approved design**

Write the design doc with scope, non-goals, affected areas, risks, and verification commands.

**Step 2: Record the implementation plan**

Write this execution plan before touching production files.

**Step 3: Verify files exist**

Run: `ls docs/plans | rg "2026-03-06-remove-llm247-v1"`

Expected: both plan files are listed.

### Task 2: Remove legacy source and runtime scripts

**Files:**
- Delete: `src/llm247/__init__.py`
- Delete: `src/llm247/__main__.py`
- Delete: `src/llm247/ark_client.py`
- Delete: `src/llm247/autonomous.py`
- Delete: `src/llm247/config.py`
- Delete: `src/llm247/context.py`
- Delete: `src/llm247/daemon.py`
- Delete: `src/llm247/dashboard.py`
- Delete: `src/llm247/reports.py`
- Delete: `src/llm247/runtime_codes.py`
- Delete: `src/llm247/scheduler.py`
- Delete: `src/llm247/storage.py`
- Delete: `src/llm247/tasks.py`
- Delete: `src/llm247/token_usage.py`
- Delete: `src/llm247/worker.py`
- Delete: `scripts/start.sh`
- Delete: `scripts/recover_autonomous.py`

**Step 1: Remove V1 package and scripts**

Delete the listed files.

**Step 2: Verify the runtime entry points are gone**

Run: `test ! -e scripts/start.sh && test ! -d src/llm247`

Expected: exit code 0.

### Task 3: Remove legacy tests

**Files:**
- Delete: `tests/test_ark_client.py`
- Delete: `tests/test_autonomous.py`
- Delete: `tests/test_config.py`
- Delete: `tests/test_core.py`
- Delete: `tests/test_daemon.py`
- Delete: `tests/test_dashboard.py`
- Delete: `tests/test_logging.py`
- Delete: `tests/test_tasks.py`
- Delete: `tests/test_worker.py`

**Step 1: Delete tests that exercise V1**

Remove all legacy tests that import `llm247`.

**Step 2: Verify only V2 tests remain**

Run: `ls tests | sort`

Expected: no remaining legacy test file names listed above.

### Task 4: Update repository documentation and conventions

**Files:**
- Modify: `AGENTS.md`
- Modify: `README.md`

**Step 1: Rewrite V2 source convention**

Change the project convention from “V1 preserved for reference” to “V2 is the only supported agent implementation”.

**Step 2: Rewrite the test naming convention**

Remove coexistence language and document that all maintained tests now target V2.

**Step 3: Add a project-specific note about the removal**

Record the repository decision and reason in the project-specific section.

**Step 4: Remove V1 references from README**

Ensure README only documents V2/Sprout usage.

### Task 5: Verify repository is V2-only

**Files:**
- Modify if needed: any file still containing stale V1 references

**Step 1: Run V2 test suite**

Run: `PYTHONPATH=src python3 -m unittest discover -s tests -p "test_v2_*.py" -v`

Expected: all V2 tests pass.

**Step 2: Rebuild frontend**

Run: `npm run build`

Workdir: `frontend/`

Expected: Vite build completes successfully.

**Step 3: Scan for stale V1 references**

Run: `rg -n "\\bllm247\\b|src/llm247|test_(ark_client|autonomous|config|core|daemon|dashboard|logging|tasks|worker)\\.py" AGENTS.md README.md scripts src tests docs`

Expected: no remaining supported-path references to deleted V1 runtime or tests.

**Step 4: Review git diff**

Run: `git diff --stat`

Expected: diff shows V1 deletions plus V2-only doc updates, with no accidental unrelated edits.
